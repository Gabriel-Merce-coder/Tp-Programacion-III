const Navbar = () => {
    return(
        <nav className = "navbar">
            <div className = "container">
                <a className = "navbar-inicio">Inicio</a>
            </div>
        </nav>
    )
}

export default Navbar;